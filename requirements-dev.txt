# Nur fuer Tests auf dem Entwicklungsrechner / in der CI.
# NICHT im Cloud-Run-Image installieren -- das nutzt requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
-r requirements.txt
pytest>=9.1.1
pytest-cov>=7.1.0
